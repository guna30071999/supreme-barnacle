void plusMinus(int arr_count, int* arr) {
int i;
float x=0,y=0,z=0;
for(i=0;i<arr_count;i++)
{
    if(arr[i]>0)
        x+=1;
    else if(arr[i]<0)
        y+=1;
    else
        z+=1;
}
x=x/arr_count;
y=y/arr_count;
z=z/arr_count;
printf("%.6f\n%.6f\n%.6f",x,y,z);
}
